import re

def generate_ass(txt_path, ass_path, translations):
    with open(txt_path, 'r', encoding='utf-8') as f:
        lines = f.readlines()

    ass_header = """[Script Info]
; Script generated by Antigravity
Title: Claude Code Lesson 10 Bilingual Subtitles
ScriptType: v4.00+
WrapStyle: 0
ScaledBorderAndShadow: yes
YCbCr Matrix: TV.709
PlayResX: 1920
PlayResY: 1080

[V4+ Styles]
Format: Name, Fontname, Fontsize, PrimaryColour, SecondaryColour, OutlineColour, BackColour, Bold, Italic, Underline, StrikeOut, ScaleX, ScaleY, Spacing, Angle, BorderStyle, Outline, Shadow, Alignment, MarginL, MarginR, MarginV, Encoding
Style: Default,Arial,20,&H00FFFFFF,&H000000FF,&H00000000,&H00000000,0,0,0,0,100,100,0,0,1,2,2,2,10,10,50,1

[Events]
Format: Layer, Start, End, Style, Name, MarginL, MarginR, MarginV, Effect, Text
"""

    pattern = re.compile(r'\[(\d+):(\d+)\] (.*)')
    dialogues = []
    
    for i in range(len(lines)):
        match = pattern.match(lines[i].strip())
        if match:
            m = int(match.group(1))
            s = int(match.group(2))
            text_en = match.group(3).strip()
            
            # Format time HH:MM:SS.CC
            h = m // 60
            rm = m % 60
            start_time = f"{h}:{rm:02}:{s:02}.00"
            
            # Find next timestamp for end time
            end_time = None
            if i + 1 < len(lines):
                next_match = pattern.match(lines[i+1].strip())
                if next_match:
                    nm = int(next_match.group(1))
                    ns = int(next_match.group(2))
                    nh = nm // 60
                    nrm = nm % 60
                    end_time = f"{nh}:{nrm:02}:{ns:02}.00"
            
            if not end_time:
                end_time = f"{h}:{rm:02}:{(s+3):02}.00" # Default 3s for last line

            text_zh = translations.get(text_en, text_en)
            dialogues.append(f"Dialogue: 0,{start_time},{end_time},Default,,0,0,0,,{{\\fs80}}{text_zh}\\N{{\\fs50}}{text_en}")

    with open(ass_path, 'w', encoding='utf-8') as f:
        f.write(ass_header)
        for d in dialogues:
            f.write(d + "\n")

if __name__ == "__main__":
    lesson10_translations = {
        "Congratulations on making it this far.": "恭喜你走到了这一步。",
        "You've learned how to use Claude Code": "你已经学习了如何使用 Claude Code",
        "to explore, test, refactor, and debug code bases.": "来探索、测试、重构和调试代码库。",
        "You can make the best out of Claude Code": "为了充分发挥 Claude Code 的威力，",
        "when you give it clear instructions,": "请给它清晰的指令，",
        "clarify the context,": "明确当前的上下文信息，",
        "and point it to the relevant files in your codebase.": "并指向代码库中相关的特定文件。",
        "Make sure to add your codebase rules to CLAUDE.md": "请务必将你的代码库规范添加到 CLAUDE.md 文件中，",
        "and any information that you'd like": "以及任何你希望",
        "Claude Code to remember about your project.": "Claude Code 记住的项目信息。",
        "Consider extending Claude Code capabilities": "你可以考虑扩展 Claude Code 的能力，",
        "and connecting it to MCP servers like Playwright and Figma.": "将其连接到如 Playwright 和 Figma 等 MCP 服务器。",
        "Thank you for joining me on this journey.": "感谢你与我一起开启这段旅程。",
        "and I can't wait to see what you'll build with Claude Code.": "我迫不及待地想看到你使用 Claude Code 构建出的作品。"
    }

    generate_ass(
        r'd:\OneDrive\Notes\deeplearning.ai\Claude Code A Highly Agentic Coding Assistant\transcripts\10_conclusion.txt',
        r'd:\OneDrive\Notes\deeplearning.ai\Claude Code A Highly Agentic Coding Assistant\subtitles\10_conclusion.ass',
        lesson10_translations
    )
    print("Lesson 10 ASS generation complete.")

# 课程讲稿：第八节 - 重构 Jupyter Notebook 并创建仪表盘（全文翻译）

在本节课中，我们将通过第二个实战案例，学习如何使用 Claude Code 来重构 Jupyter notebook 并将其转换为一个交互式仪表盘。让我们开始吧。

到目前为止，我们已经看到了如何使用 Claude Code 处理前端和后端的 Web 应用程序。现在，让我们切换一下方向，展示如何使用 Claude Code 来处理 Jupyter notebook。

我这里有一个 Jupyter notebook，包含了一些虚构的电子商务数据。这些数据是从我 `ecommerce_data` 文件夹里的几个 CSV 文件中读取的。我正在使用 pandas 读取这些 CSV 数据。

我在这里展示了一些信息，读取了其他 CSV，如你所见，我得到了我想要的答案，但这些数据组织得并不理想。我得到了一些统计信息，但格式和展示效果并非我开箱即用的理想状态。

随着我们继续深入，你可以看到我开始进行一些更复杂的操作。我希望从这些数据中获得一些洞察，比如我们的年同比收入增长情况，特别是 2023 年与 2022 年的对比。

我这里有实现目标所需的必要操作。事实上，在两个不同的 CSV 和数据帧（data frames）之间进行了一些合并。我正在朝着我想要的答案努力，但我在这里看到了一些警告。同时，仅仅为了直观展示我想要的内容及其最佳显示方式，就花费了很长时间。

虽然我最终得到了想要的数字，但这有点枯燥且凌乱。我想要一种更好的方式来展示这些信息，并探索如何重构这个 Jupyter notebook。

正如我们在其他部分看到的那样，我正试图计算一些高层指标，如平均订单价值（AOV），我不只是想要这个数字，而且这只是 2023 年的数据。如果我想要 2024 年甚至以后的数据该怎么办？

当我们向下滚动时，我们会看到一些还不错的可视化效果。但我认为我们可以做得更好。当我们快到底部时，我们仍在计算更多的高层数值，如按州划分的销售额。我们再次生成了一些可视化效果，但我认为在使用 Claude Code 等工具后，我们可以做得更好。

考虑到这一点，让我们展示如何使用 Claude Code 来重构这个 notebook，并更好地将业务逻辑与展示逻辑分离。

我要打开 Claude，并输入一个提示词，其中引用了该 notebook 以及我正在处理的其他文件。我将粘贴这个提示词，然后聊聊这里到底发生了什么。

在粘贴提示词之前，我实际上要新建一个 Markdown 文件，只是为了展示它的样子，并解释我们为什么要这样做。

注意看这里，我使用 `@` 符号引用了文件名，也引用了文件夹名。**如果你不确定如何最好地编写提示词或如何要求 Claude 执行任务，你也可以直接问 Claude。**很多时候，我发现自己在使用 Claude AI 甚至 Claude Code 时，会直接问：“我想做这件事，请给我一个完成此任务的最佳提示词。”

在我们的案例中，我们对布局结构有一些**重构要求**。我们对特定的 Python 文件有一些**代码质量改进**和要求。

我们要求 Claude 在这里创建一个独立的 Python 文件用于加载和处理数据。然后再创建另一个 Python 文件用于计算这些指标。这种**关注点分离（Separation of Concerns）**将使我们能够更好地测试并理解支撑我们在重构后的 notebook 中创建的可视化效果的代码。

我们还要求改进可视化效果，并具体说明了我们的要求。最后，我们希望对正在处理的数据进行更多的配置。

**一般来说，让 Claude 知道你的预期是一个好习惯。**所以不要只说“重构这个”，而是说“给我一个重构后的 notebook 的底层资源，给我一种记录函数和计算业务指标的方法，然后再给我一些用于处理依赖关系的重要文件以及一份说明如何使用它的 README”。

让我们复制并粘贴进去。你会注意到，粘贴一定数量的行后，Claude Code 为了保持终端简洁，不会显示所有内容。让我们运行这个提示词。

就像我们使用其他工具一样，我们在这里将使用稍微不同的工具集。我们不只是读取文件，而是处理 **notebook 读取**。但我们之前看到的待办清单（to-do list）将像我们已经习惯的那样被执行。

这里我们使用了 `read-notebook` 工具。我们将探索那些单元格，分析结构，并创建我们期望的重构后的 notebook。完成后，我们不仅能看到强大的可视化效果，还能看到一些包含助手函数的其他文件，用于加载数据、处理数据以及计算我们需要的业务指标。

接受对这些 Python 文件的更改，我们会看到 Python 代码正通过正确的工具为我们编写。然后我们将编写另一个包含指标计算的 Python 文件，用于最终的数据和展示。随着 Claude Code 完成这些独立任务，我们可以随时检查已创建的文件。

我们可以看一眼，开箱即用的这种面向对象的方法比尝试将所有内容直接嵌入到我们的 notebook 中要整洁得多。业务指标计算也是如此，我们可以看到计算能力、传入数据帧并返回可视化效果以及对数据的验证。

看来我们已经创建了业务指标，最后是我们的 notebook、`requirements.txt`，很快还会有我们的 README。

在运行的同时，让我们看看重构后的 notebook。让我们确保可以按预期运行所有单元格。

从视觉上看，这马上就显得好多了。我看到它试图读取提示词，我会告诉 Claude：“不需要读取提示词，直接完成 README 即可。”

当我查看这个 notebook 时，我发现它好得多了。我得到了对正在查看的数据的分析，还有一些可以操作的配置。我可以指定年份和对比数据，甚至可以指定月份。我将在整个 notebook 中使用这些常量。

这里我可以看到所需的数据加载，这些是我类中的自定义模块。当我运行所有这些单元格时，到目前为止看起来都很棒。

但如果我再往下滚一点，实际上会看到这里有一个错误。我一眼就能看出问题似乎是某种 `KeyError`。所以我要让 Claude 帮我修复。我会说：“运行特定单元格时出现了 `KeyError`。” 我本可以让 Claude Code 自己查出来，但我实际上会把刚才运行的代码给它，这样它就有更多的上下文。

Claude Code 将查看需要执行的操作，找出问题所在并为我们进行更改。好，现在更改已经做好了，让我们运行这些单元格。

让我们确保情况变好了一些。如你所见，当我向下滚动时，不再看到之前的错误了。现在让我们看看它返回给我的一些业务指标。

得到了更漂亮的摘要展示、关于客户满意度的信息，这比我之前拥有的要容易处理得多。

当我们转向收入分析时，我们可以开始看到更清晰的数值、展示了更多信息，并进行了一些工作以更轻松地展示数据。可视化效果看起来也更赏心悦目了。

当我们查看类别表现（category performance）时，我们有了更好的工具来分析类别及与之相关的收入。查看地理表现时，我们看到了按州划分的漂亮可视化效果。我们可以将鼠标悬停在上面，以获取各州更详细的信息。

当我们进行到最后，查看额外的交付分析时，一切都如预期般顺利。

我们在重构这个 notebook、实现更直观的数据展示以及分离关注点方面做得很好。但我现在想做的是：从 notebook 迁移到基于 Web 的界面，使用 **Streamlit** 构建一个精美的仪表盘。

如果你不熟悉 Streamlit，**它是一个用于展示界面（特别是仪表盘）的 Python 库，非常易于使用，可以无缝地从 notebook 转换为这些 Python 文件**。

让我们新建一个名为 `convert-to-dashboard.md` 的 Markdown 文件作为提示词。让我们看看要在里面放什么。我们要要求 Claude 将我们的重构后的 notebook 转换为一个专业的 Streamlit 仪表盘，并采用精确的布局。

我们可以只从第一行开始，但为了避免来回沟通，直接告诉 Claude 我们想要什么。我们需要一个带有标题和过滤器的页眉；需要带有收入、增长、平均订单价值和总订单卡片的 KPI；然后我们需要一些图表：收入、类别、按州划分的收入，以及一个显示满意度的条形图。在底层，我们需要两个卡片，并且我们要明确指定数据展示和渲染信息的方式。

考虑到这一点，让我们试一试。让我们利用 Streamlit 从 notebook 迁移到可视化界面。

我们可以看到它已经创建了我们的仪表盘，在它运行的同时，我们可以看看这里做了什么。我们正在导入 Streamlit，引入 pandas，引入包括 Plotly 在内的可视化工具。

在这里你可以看到我们正在展示用于样式设置的自定义 CSS，设置我们的页面配置。大部分内容实际上来自我用来操作 Streamlit 的 `st` 对象。我们更新了 `requirements.txt`，在这里我们实际上要创建一个展示仪表盘中所有情况的 README。事实上，我现在会告诉 Claude：“不要创建新的 README，只需修改现有的即可。”

在许多这种较长的计划中，很容易就让 Claude 自己跑，但有时你想停止进程，让它按照你想要的方式运行。既然我们已经创建了这个仪表盘，让我们打开一个新的终端窗口来启动服务器。

在左侧的终端窗口中，我将使用 `streamlit run dashboard.py` 启动服务器。我们可以看到我这里还没有 `streamlit` 命令。

所以我意识到，为了让一切运行起来，我需要安装这些依赖项。让我们开始操作。我将运行 `pip install` 命令，安装所有必要的依赖项，并确保在服务器加载时，应用程序中的依赖项不会出现任何问题。

完成后，我将启动服务器。我将使用 `streamlit run` 命令。让我们输入 `streamlit run` 以及我们 Python 文件的名称。就像它在这里建议的一样。

回到浏览器，这就是现在的样子。我得到了一些收入数据，一些增长数据。当我们稍微往下滚一点时，我们肯定要验证一下这些数字是否符合我们的预期。我们看到有些东西还缺了一点。

当我们查看这个仪表盘时，这是一个非常好的开始，但我们可以看到还缺少几样东西。看起来这些卡片是空的，实际上也是多余的，因为我们已经得到了想要的信息。

同时，从 2024 年开始显示有点困难，因为我们看不到太多的年同比信息。所以也许我们应该要求 Claude 不仅清理这些部分，还要将默认年份设为 2023 年，这样我们可以看到一些稍微漂亮的可视化效果，同时确保我们有能力基于月份进行过滤。

我们想要从 2023 年开始的原因是，我们还没有 2024 年每个月的数据。所以当我们开始查看时，月度增长有点令人困惑。而这里的数据，我们还没有得到。

所以让我们提出这些要求，把它们传回给 Claude。这是一个好的开始，但我想要实施以下更改：

让我们在这里另起一行，首先我们要确保默认年份是 2023 年。接下来，要求 Claude 也添加按月份过滤的功能。最后，我们要确保移除每个空的卡片。

好，让我们在仪表盘中为这些特定任务更新代码。我们将默认年份设置为 2023 年，这看起来不错。在这里我们添加了一些月份过滤器。现在让我们更新收入图表，以处理按特定月份过滤的情况。我们可以看到 CSS 也在发生变化。

所以看起来这个图表容器应该比以前更整洁了。让我们回到浏览器，看看情况。我们可以看到 2023 年是默认设置，这是一个好的开始。我们还可以看到那些空的卡片已经不在了，这太棒了。

我们现在先停在这里，但你可以想象，随着你不断通过提示词引导，你可以创建更强大的可视化效果。但在很短的时间内，我们从一个有点混乱、难以阅读和理解的 notebook，迁移到了一个我们可以部署、与团队成员分享并快速迭代的仪表盘。

在下一节课中，我们将构建一个 Next.js 应用程序，并使用 Claude Code 连接到 Figma 和 Playwright 的 MCP 服务器，从而利用原型图（mockup）通过 Claude Code 快速构建一个功能强大的 Web 应用程序。
